apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudnative-devopslab.fullname" . }}-config
  labels:
    {{- include "cloudnative-devopslab.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.config | nindent 2 }}
